apiVersion: v1
kind: Service
metadata:
  name: apexpos-frontend-svc
  namespace: apexpos
spec:
  type: NodePort
  selector:
    app: apexpos-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
